# Disclosure data lives outside version control — it's bulky, regenerable
# (`openhouse pull`/`parse`), and carries the §1 legal use restriction.
data/

.DS_Store
.env
.idea
.venv
.vscode
__pycache__
*.py[oc]
*.swo
*.swp
*.egg-info
build
dist
wheels/
Thumbs.db

# Frontend (web/inspect) contributor build deps. The *built* bundle is committed
# under openhouse/inspect/static (not dist/, which is ignored above) so
# `openhouse inspect` runs with zero Node at runtime.
node_modules/

# .claude/ holds both shared tooling (tracked below) and personal/transient
# state (ignored). Ignore everything, then re-include the shared artifacts.
.claude/*
!.claude/agents/
!.claude/hooks/
!.claude/settings.json
!.claude/skills/
# Per-repo config for the vendored ship skill (authored here, never pressed).
# The skill reads this; everything repo-specific lives in it.
!.claude/ship.toml
# Provenance stamp left by the press: machine-local (drawer path + file hashes),
# so a clone with no drawer self-skips its drift check. Never committed.
.claude/skills/*/.stamp
.claude/scratch

# Playwright MCP scratch output (browser console logs + page snapshots)
.playwright-mcp/

# Parse-validation sweep outputs are bulky and regenerable — ignore them, but
# keep the hand-maintained per-run results log (brief summaries, see
# docs/parse-validation-sweep.md).
reports/*
!reports/parse-validation-sweep-results.md
